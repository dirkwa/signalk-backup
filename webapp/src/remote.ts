// Build input for the federated remote. The real entry, remoteEntry.js, is
// emitted by @module-federation/vite; rolldown just needs an input, and
// vite.config.ts explains why it must not be the dev shell (dev.html).
export {}
